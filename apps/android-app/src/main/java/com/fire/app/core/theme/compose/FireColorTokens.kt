package com.fire.app.core.theme.compose

import androidx.compose.ui.graphics.Color

internal object FireColorTokens {
    val lightAccent = Color(0xFFE8632E)
    val lightAccentSoft = Color(0xFFFAA666)
    val lightAccentGlow = Color(0xFFFCD1AD)

    val darkAccent = Color(0xFFFA7A3D)
    val darkAccentSoft = Color(0xFFFF9E66)
    val darkAccentGlow = Color(0xFFFFB885)

    val lightSuccess = Color(0xFF40A173)
    val lightWarning = Color(0xFFCC7D33)
    val lightError = Color(0xFFE64738)
    val lightInfo = Color(0xFF337AF5)

    val darkSuccess = Color(0xFF61C78C)
    val darkWarning = Color(0xFFF29E4D)
    val darkError = Color(0xFFFF6147)
    val darkInfo = Color(0xFF669EFF)

    val lightCanvasTop = Color(0xFFF5F2ED)
    val lightCanvasMid = Color(0xFFF2F0EB)
    val lightCanvasBottom = Color(0xFFEDEBE8)

    val darkCanvasTop = Color(0xFF000000)
    val darkCanvasMid = Color(0xFF000000)
    val darkCanvasBottom = Color(0xFF000000)

    val lightSurface = Color(0xFFFFFFFF)
    val lightSurfaceSecondary = Color(0xFFF2F0ED)
    val lightPanel = Color(0xFFFFFFFF)
    val lightPanelElevated = Color(0xFFFFFFFF)
    val lightIconWell = Color(0xFFF0EDEB)
    val lightChrome = Color(0xF0F2F0EB)
    val lightChromeStrong = Color(0xFAF2F0EB)
    val lightSoftSurface = Color.White.copy(alpha = 0.56f)
    val lightTrack = Color(0xFFEBE8E6)

    val darkSurface = Color(0xFF1C1C1E)
    val darkSurfaceSecondary = Color(0xFF2C2C2E)
    val darkPanel = Color(0xFF1C1C1E)
    val darkPanelElevated = Color(0xFF2C2C2E)
    val darkIconWell = Color(0xFF121213)
    val darkChrome = Color(0xEB141417)
    val darkChromeStrong = Color(0xF51F1F21)
    val darkSoftSurface = Color.White.copy(alpha = 0.06f)
    val darkTrack = Color.White.copy(alpha = 0.10f)

    val lightInk = Color(0xFF1C1C1F)
    val lightSubtleInk = Color(0xFF66666E)
    val lightTertiaryInk = Color(0xFF8C8C94)
    val lightInverseInk = Color(0xFFFAF5ED)
    val lightInverseSubtleInk = Color(0xFFC7BDB0)

    val darkInk = Color.White.copy(alpha = 0.96f)
    val darkSubtleInk = Color.White.copy(alpha = 0.55f)
    val darkTertiaryInk = Color.White.copy(alpha = 0.38f)
    val darkInverseInk = Color(0xFF1F1F21)
    val darkInverseSubtleInk = Color(0xFF66666B)

    val lightDivider = Color.Black.copy(alpha = 0.08f)
    val lightChromeBorder = Color.White.copy(alpha = 0.40f)
    val lightInverseDivider = Color.White.copy(alpha = 0.10f)
    val lightThreadLine = Color.Black.copy(alpha = 0.10f)
    val lightPanelShadow = Color.Black.copy(alpha = 0.06f)
    val lightContrastPanelShadow = Color.Black.copy(alpha = 0.14f)

    val darkDivider = Color.White.copy(alpha = 0.08f)
    val darkChromeBorder = Color.White.copy(alpha = 0.08f)
    val darkInverseDivider = Color.White.copy(alpha = 0.10f)
    val darkThreadLine = Color.White.copy(alpha = 0.08f)
    val darkPanelShadow = Color.Black.copy(alpha = 0.40f)
    val darkContrastPanelShadow = Color.Black.copy(alpha = 0.55f)

    val lightTagChipBackground = Color(0xFF757580).copy(alpha = 0.08f)
    val lightTagChipForeground = Color(0xFF4D4D54)

    val darkTagChipBackground = Color.White.copy(alpha = 0.08f)
    val darkTagChipForeground = Color.White.copy(alpha = 0.72f)

    val lightSkeletonBase = Color(0xFFE6E6E6)
    val lightSkeletonHighlight = Color(0xFFF5F5F5)

    val darkSkeletonBase = Color(0xFF292929)
    val darkSkeletonHighlight = Color(0xFF424242)

    val lightTabBarBackground = Color(0xF5F2F0EB)
    val darkTabBarBackground = Color(0xF00D0D0D)
}
