package com.fire.app.core.theme.compose

import androidx.compose.foundation.shape.RoundedCornerShape

object FireShapes {
    val card = RoundedCornerShape(FireDimens.cornerRadius)
    val cardMedium = RoundedCornerShape(FireDimens.mediumCornerRadius)
    val smallControl = RoundedCornerShape(FireDimens.smallCornerRadius)
    val iconWell = RoundedCornerShape(FireDimens.iconWellCornerRadius)
    val chip = RoundedCornerShape(FireDimens.chipCornerRadius)
}
